<% if category.errors %>
  <ul>
    <% category.errors.full_messages.each do |e| %>
      <li><%= e %></li>
    <% end %>
  </ul>
<% end %>
<%= form_for category do |f| %>
  <%= f.label "Category Name:" %>
  <br />
  <%= f.text_field :name %>
  <br />
  <br />
  <%= f.submit "Submit" %>
<% end %>
