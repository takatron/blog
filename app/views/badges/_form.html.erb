<% if badge.errors %>
  <ul>
    <% badge.errors.full_messages.each do |e| %>
      <li><%= e %></li>
    <% end %>
  </ul>
<% end %>
<%= form_for badge do |f| %>
  <%= f.label "Badge Name:" %>
  <br />
  <%= f.text_field :name %>
  <br />
  <br />
  <%= f.submit "Submit" %>
<% end %>
